/* Template for the gate passphrase.
   Copy this file to gate.config.js (which is gitignored) and set your own value.
   NOTE: this is a client-side gate for a private preview only — the value
   is downloaded by the browser and is NOT hidden from users. */
window.GTL_PASSPHRASE = 'changeme';
